>The matrix in ./test.js at line 40 might be incorrect.
>The value representation in ./public/index.html at line 23 might be incorrect.

<thoughts>
  When assuming the parsed data matrix (array dataMatrix) is:
  [
    [ d_1-1 , d_1-2 , d_1-3 , ... , d_1-8640 ],
    [ d_2-1 , d_2-2 , d_2-3 , ... , d_2-8640 ],
    [ d_3-1 , d_3-2 , d_3-3 , ... , d_3-8640 ],
    .
    .
    .
    [ d_3432-1 , d_3432-2 , d_3432-3 , ... , d_3432-8640 ]
  ]
  , then when the data is parsed from d_3432-1 to d_1-8640 ** ; The output visualization looks like 1.png which corelates to imaging
  by Nasa i.e nasa1.png (Site link is given at end).
  But when parse from d_1-1 to d_3432-8604 *** ; The output visualization looks like 2.png which corelates to imaging by Nasa i.e nasa2.png

</thoughts>

link: https://search.earthdata.nasa.gov/search/granules?p=C179001770-SEDAC&pg[0][gsk]=-start_date&g=G1397320629-SEDAC&m=-40.50321462679236!-164.25!0!1!0!0%2C2&ff=Map%20Imagery&tl=1583820684!4!!&fst0=Human%20Dimensions

footnotes:
**
```js
const latnum = Math.ceil((-basicData[3]+latitude)/basicData[4]), longnum = Math.ceil((-basicData[2]+longitude)/basicData[4]);

for(let i = basicData[1]-1; i > 0; i--){
  for(let index = 0; index < basicData[0]; index++){
    dataMatrix[i][index]=[ dataValues[i][index],[basicData[2]+basicData[4]*index , basicData[3]+ basicData[4]*i] ]
    if(dataValues[i][index] != -1) console.log(JSON.stringify(dataMatrix[i][index]));
  }
}
```

***
```js
const latnum = Math.ceil((85-latitude)/basicData[4]), longnum = Math.ceil((-basicData[2]+longitude)/basicData[4]);

for(let i = 0; i < basicData[1]; i++){
  for(let index = 0; index < basicData[0]; index++){
    dataMatrix[i][index]=[ dataValues[i][index],[basicData[2]+basicData[4]*index , 84.999 - basicData[4]*i] ]
    if(dataValues[i][index] != -1) console.log(JSON.stringify(dataMatrix[i][index]));
  }
}
```
