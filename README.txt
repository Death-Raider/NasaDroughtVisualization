extract the gddrg.rar file to the same directory as test.js and server.js
run server.js and open localHost at port 3000.
test.js parses the gddrg.asc and send data to server which sends it to webpage.
